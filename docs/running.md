# Running the Project

This repository does not yet contain an application entry point. Today, "running" it means reviewing the documentation under `docs/` and optionally running the validation script:

```bash
python tests/validate_repo.py
```

Update this file with the real run command once application code is added.
