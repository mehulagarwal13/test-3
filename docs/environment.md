# Environment Configuration

No environment variables, API keys, or secrets are required to work on this repository today.

If a future change introduces configuration:

- Document each required variable's *name* and *purpose* here.
- Never commit a real value -- provide a `.env.example` with placeholder values instead.
- Keep real `.env` files out of version control via `.gitignore`.
